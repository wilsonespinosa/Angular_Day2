import { SuffixFilterPipe } from './suffix-filter.pipe';

describe('SuffixFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SuffixFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
